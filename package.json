{
  "name": "blog",
  "version": "1.0.0",
  "description": "Chad Voegele's Blog",
  "scripts": {
    "copy": "posts/suppalist/copy.mjs",
    "build": "npm run copy && npx eleventy",
    "serve": "npm run copy && npx eleventy --serve",
    "debug": "npm run copy && DEBUG=* npx eleventy"
  },
  "author": "Chad Voegele",
  "license": "MIT",
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
